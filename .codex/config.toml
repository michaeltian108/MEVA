# Reusable MEVA project configuration.
# Merge these documented keys into an existing trusted project configuration.

[agents]
enabled = true
max_concurrent_threads_per_session = 4
interrupt_message = true
